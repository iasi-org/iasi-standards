services:

  # PlantUML Server / MCP
  plantuml:
    image: ${PLANTUML_IMAGE}:${PLANTUML_TAG}
    container_name: ${CONTAINER_NAME}
    restart: ${RESTART_POLICY}
    ports:
      - "${PLANTUML_PORT}:8080"